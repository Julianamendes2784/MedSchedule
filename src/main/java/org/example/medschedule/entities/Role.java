package org.example.medschedule.entities;

/**
 * Perfis de acesso possíveis (médico ou administrador).
 * Ainda não é usado por nenhuma entidade; fica reservado para a fase de Segurança/permissões.
 */
public enum Role {
    MEDICO,
    ADMIN
}
